#! /usr/bin/env loki

loki-set-log-prefix "FDI: "

name=$(basename "$1")

for item in $(fenrir-read-conf -a forbidden directories); do
    if [ "$name" = "$item" ]; then
        loki-log err "Found a forbidden directory: '$1'"
        exit 3
    fi
done
