#! /bin/bash

verbosity=$1
branch=$2


fenrir-log info "$verbosity" "Checking whether branch <$branch> exists.."

if git branch | grep -q "$branch"; then
    fenrir-log succ "$verbosity" "Branch exists"
else
    fenrir-log err "$verbosity" "Branch does not exist"
    exit 1
fi
