#! /usr/bin/env loki

verbosity=$1
branch=$2


loki-log info "$verbosity" "Checking whether branch <$branch> exists.."

if git branch | grep -q "$branch"; then
    loki-log succ "$verbosity" "Branch exists"
else
    loki-log err "$verbosity" "Branch does not exist"
    exit 1
fi
