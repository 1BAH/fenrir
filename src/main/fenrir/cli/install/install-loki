# shellcheck disable=SC2164
pushd "$BUILD_DIR/loki" &> /dev/null

./install.loki

sudo cp LOKI_ASSEMBLED "${FENRIR_INSTALL_DIR}/loki"
sudo chmod +x "${FENRIR_INSTALL_DIR}/loki"

source "${FENRIR_INSTALL_DIR}/loki"

popd &> /dev/null
