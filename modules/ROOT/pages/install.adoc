= Install
:page-pagination:

Debian-like Linux::
[,console]
----
$ sudo mkdir -p /usr/share/keyrings
$ curl -fsSL https://1bah.github.io/public.key | sudo gpg --dearmor -o /usr/share/keyrings/1bah.gpg
$ echo 'deb [signed-by=/usr/share/keyrings/1bah.gpg] https://1bah.github.io/deb stable main' | sudo tee /etc/apt/sources.list.d/1bah.list
$ sudo apt update && sudo apt install fenrir-1bah
----

RHEL-like Linux::
[,console]
----
$ cat << 'CONF' | sudo tee /etc/yum.repos.d/1bah.repo
[1bah-repo]
name=1BAH package registry
baseurl=https://1bah.github.io/rpm
enabled=1
gpgcheck=1
gpgkey=https://1bah.github.io/public.key
CONF
$ sudo dnf install fenrir
----

[#other]
Other::
[,console]
----
$ git clone https://github.com/1BAH/fenrir.git /tmp/frir
$ cd /tmp/frir/src/main/fenrir/cli
$ sudo ./install-fenrir
----

Verify the installation:
[,console]
----
$ fenrir i
  _____               _
 |  ___|__ _ __  _ __(_)_ __
 | |_ / _ \ '_ \| '__| | '__|
 |  _|  __/ | | | |  | | |
 |_|  \___|_| |_|_|  |_|_|

------------------------------------------------------------------
Current settings:
    * Remote: https://github.com/1BAH/fenrir-tests
    * Current task: -
    * Blocking: 0
    * Execution mode: 111 (Inline-Host-Docker)
------------------------------------------------------------------
Run settings:

------------------------------------------------------------------
fenrir 2026.0+1, 2026
@1BAH
----

MacOS::

. Update `bash` with `brew` (must be v5.0+)
. Install `gsed`
. Follow the <<other, other>> guidelines

Windows::

You cannot use fenrir directly in `Windows` that's why we recommend to use https://learn.microsoft.com/en-us/windows/wsl/install[WSL]. Chose any distro and use installs guides above.
